#!/usr/bin/python3
"""module documentation for using isinstance"""


def is_same_class(obj, a_class):
    """defines use if instance"""

    if isinstance(obj, a_class):
        return True
    else:
        return False
